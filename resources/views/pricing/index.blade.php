@extends('layout')
@section('content')
<div class="page-head"><div><div class="eyebrow">Dubai price desk</div><h1>Prices needed</h1><p>Tap a request, enter each unit price, and send it back to Muscat.</p></div></div>
<div class="pricing-list">@forelse($quotations as $q)<a class="pricing-job pricing-job-link" href="{{ route('pricing.edit',$q) }}"><div class="pricing-job-main"><div class="pricing-company"><span class="company-dot" style="background:{{ $q->company->accent }}"></span><strong>{{ $q->company->trading_name }}</strong></div><span class="badge {{ $q->status }}">{{ $q->status==='pricing'?'Needs prices':'Completed' }}</span><h3>{{ $q->number }}</h3><p>{{ $q->customer_name }}{{ $q->customer_company ? ' · '.$q->customer_company : '' }}</p><small class="muted">{{ $q->items->count() }} products · requested by {{ $q->creator->name }} · {{ $q->created_at->format('d M Y, H:i') }}</small></div><span class="btn {{ $q->status==='pricing'?'green':'secondary' }}">{{ $q->status==='pricing'?'Open request':'Review' }} <span aria-hidden="true">→</span></span></a>
@empty<div class="panel form-panel"><h2>You're all caught up</h2><p class="muted">New pricing requests from Muscat will appear here.</p></div>@endforelse</div>
@endsection
